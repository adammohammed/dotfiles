;;; early-init.el --- Basic Bootstrapping
;;; Commentary:
;;
;; This just turns off `package.el' in favor of `straight.el'.

;;; Code:
(setq package-enable-at-startup nil)

(provide 'early-init)
;;; early-init.el ends here
